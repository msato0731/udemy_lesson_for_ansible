# Lesson fo Ansible

Ansible学習用プロジェクト

## 仮想サーバー

- provisioningサーバー  
Ansibleを実行するサーバー

- sandboxサーバー  
Ansibleの実行対象となるサーバー

## 仮想サーバーの起動方法

- provisioningサーバー  
```
$ cd VMs/provisioning
$ vagrant up
```

- sandboxサーバー  
```
$ cd VMs/sandbox
$ vagrant up
```